import React, { useEffect, useRef, useState } from "react";

const Main: React.FC = () => {
  useEffect(() => {
    window.addEventListener("mousedown", () => {});
  }, []);
  return <></>;
};

export default Main;
